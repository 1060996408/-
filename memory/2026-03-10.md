# 2026-03-10

## 08:36 系统健康检查

执行了 `openclaw doctor` 诊断，发现以下问题：

### 需要关注的问题

1. **内存搜索功能未配置** - 缺少 embedding provider，语义检索功能无法使用
   - 需要配置 OPENAI_API_KEY、GEMINI_API_KEY、VOYAGE_API_KEY 或 MISTRAL_API_KEY
   - 或者禁用该功能

2. **安全警告** - Gateway 绑定到 LAN (0.0.0.0)，网络可访问
   - 建议使用 Tailscale 或 SSH 隧道进行远程访问
   - 或运行 `openclaw security audit --deep` 进行深度安全审计

3. **启动优化建议** - 可设置环境变量提升性能
   ```bash
   export NODE_COMPILE_CACHE=/var/tmp/openclaw-compile-cache
   mkdir -p /var/tmp/openclaw-compile-cache
   export OPENCLAW_NO_RESPAWN=1
   ```

### 正常状态

- 会话锁文件正常 (2个活跃)
- 技能状态: 30个可用，36个缺少依赖
- 插件: 4个已加载，34个已禁用
- 4个 Agent 运行正常: orchestrator, coder-maintainer, researcher, knowledge-weaver
- 心跳间隔: 30分钟

### 建议操作

1. 配置 embedding provider 以启用语义搜索
2. 运行 `openclaw security audit --deep` 检查安全配置
3. 考虑应用启动优化设置

## 09:10 系统问题修复

### 已解决

✅ **Node.js 版本** - 实际已是 v22.22.1 (满足要求)
✅ **npm 权限** - prefix 已正确配置到 ~/.npm-global
✅ **安全配置** - 已修复:
   - 禁用 dangerouslyAllowHostHeaderOriginFallback
   - 添加 allowedOrigins: localhost:18790, 127.0.0.1:18790
   - 配置 auth.rateLimit (10次/分钟, 锁定5分钟)
✅ **危险 skill** - 已删除 tavily-search (存在 env-harvesting 风险)
✅ **启动优化** - 已添加环境变量到 ~/.bashrc

### 待处理

⚠️ **内存搜索功能** - 需要 embedding provider API key (可选)
⚠️ **Skills 依赖** - 36个 skills 缺少依赖 (需要时再安装)

### 配置变更

已通过 config.patch 更新配置并重启 gateway (PID 26167, SIGUSR1)
